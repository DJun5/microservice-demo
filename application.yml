# 获取配置文件信息规律：
# 1、http://{url}:{port}/application-{profiles}
# 2、

spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev      # 开发环境
  application:
    name: microservice-config-dev
---
spring:
  profiles: test     # 测试环境
  application:
    name: microservice-config-test